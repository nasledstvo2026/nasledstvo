# ТЗ: Починка чанкера для 425-ФЗ

## Проблема
Чанкер извлёк заголовки статей 425-ФЗ, но текст потерял. Причина: разметка КонсультантПлюс (таблицы `+---+`, примечания, ссылки) сбивает парсер.

## Шаг 1. Диагностика
Открыть `chunk_laws.py`. Баг в том, как парсер определяет начало/конец текста статьи — для НК это работает (простой markdown), а для 425-ФЗ КонсультантПлюс-разметка ломает логику.

## Шаг 2. Починка
Перед парсингом 425-ФЗ — предобработка:
- Вырезать все строки с `+---+` (таблицы КонсультантПлюс)
- Вырезать строки с `КонсультантПлюс: примечание`
- Вырезать `![](media/...)` (картинки)
- Вырезать ссылки `[текст](url)` → оставлять текст

Убедиться что текст статьи 20 («Установить, что со дня вступления в силу...») попадает в чанк.

## Шаг 3. Пересборка
```bash
cd /home/user1/phoenix
./venv/bin/python chunk_laws.py
./venv/bin/python index_laws.py
systemctl --user restart law-search.service
sleep 35
curl 127.0.0.1:8765/health
```

## Шаг 4. Проверка
```bash
curl -sG --data-urlencode "q=переходные положения" 127.0.0.1:8765/search?k=3
```

## Ожидаемый результат
- Чанков > 738 (текст 425-ФЗ добавился)
- Статья 20 находится по запросу «переходные положения СМЭВ»
- И3 проходит
