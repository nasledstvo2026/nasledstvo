# ТЗ: Семантический поиск по 425-ФЗ + НК РФ (замена FTS)

## Контекст
FTS провалился: не находит «ПНО» (в законе «поручение налогового органа»), путает «статья 46» со «статья 197». Нужен embedding.

## Исходные данные (в workspace Феникса)
- `Федеральныи_закон_от_28_11_2025_N_425_ФЗ...md` — 5 407 строк, ~340 стр., 20 статей
- `Налоговыи_кодекс_России_скои_Федерации_часть_первая...md` — 15 251 строка, ~1 326 стр., ~200 статей
- `Налоговыи_кодекс_России_скои_Федерации_часть_вторая...md` — 52 411 строк, ~4 277 стр., ~400 статей
- **Итого: ~6 000 стр., ~620 статей**

## Шаг 1. Установка
```bash
pip3 install sentence-transformers faiss-cpu
```

## Шаг 2. Чанкинг (постатейный)

Статьи в markdown начинаются со строки `Статья X` или `Статья X.`

**Алгоритм:**
1. Вырезать мусор КонсультантПлюс: таблицы `+---+`, примечания «КонсультантПлюс:», картинки `![](media/...)`, ссылки `[текст](url)` → оставлять только текст.
2. Разбить каждый документ на чанки по границам статей.
3. Заголовок статьи включать в чанк (например: «Статья 46. Поручение налогового органа о перечислении налога»).
4. Мелкие статьи (< 100 слов) склеивать со следующей.
5. Крупные статьи не резать — хранить целиком.
6. Метаданные каждого чанка: `{source: "nk1"|"nk2"|"fz425", article: "46", title: "Поручение налогового органа..."}`

## Шаг 3. Индексация

Модель: `intfloat/multilingual-e5-small` (384-мерные векторы, ~470 MB).

**Важно для e5:** к запросу добавлять префикс `query:`, к документу — `passage:`.

Индекс: FAISS `IndexFlatIP` (inner product = cosine similarity на нормализованных векторах).

Сохранять индекс в файл `fz425_index.faiss` + метаданные в `fz425_meta.json`.

## Шаг 4. CLI-поиск

Скрипт `search_law.py`:

```bash
python3 search_law.py "ПНО срок обработки 3 часа"
# → top-5 результатов: ранг, score, источник, статья, сниппет
```

Поиск: нормализовать query-вектор → `index.search(query_vec, k=5)` → вернуть метаданные.

Интерфейс: CLI (вызов через exec из агента). Модель грузится при каждом вызове (~2-3 сек), на фоне LLM-ответа незаметно.

## Шаг 5. Тестирование

Прогнать те же 6 тестов и сравнить с FTS:

| # | Запрос | Ожидание |
|---|--------|----------|
| Т1 | «ПНО срок обработки 3 часа» | Статья про поручение налогового органа + сроки |
| Т2 | «банк обязан найти счета должника» | Ст. 46/60 НК или 425-ФЗ про розыск счетов |
| Т3 | «статья 46 НК РФ» | Именно ст. 46 НК ч.1 (не 197) |
| Т4 | «переходный период СМЭВ-3» | Ст. 20 425-ФЗ (переходные положения) |
| Т5 | «цифровой рубль платформа оператор» | Статья про цифровую валюту |
| Т6 | «крокодил Гена» | Нет релевантных результатов |

## Ожидаемый результат

Семантический поиск должен понимать:
- Аббревиатуры (ПНО → поручение налогового органа)
- Синонимы (переходный период → переходные положения)
- Контекст (статья 46 НК → именно ст. 46, а не любое упоминание слова «статья»)

**После реализации — таблица результатов тестирования (аналогичная FTS) и verdict.**
